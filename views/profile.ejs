<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatibsle" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <style>
      body{
        position: relative;
        max-width: 1440px;
        max-height: 1440px;
        background-color: #D9D9D9;
      }

      .container{
        position: absolute;
        width: 500px;
height: 420px;

background: #fff;
border-radius: 7px;
      }
        .image{
          border-radius: 60%;
        }
        .header{
          font-family: Arial, Helvetica, sans-serif;
          font-size: larger;
          margin-left: 2em
          
        
        }
        .heading{
          color: rgb(18, 17, 17);
        }
        .button2{
          background-color: rgb(65, 65, 235);
          margin-left: 12em;
          width: 8em;
          height: 4em;
          border-radius: 20px;
          border: none;
          
        
        }
        .button2:hover{
          background: #668ba6;
        }
        .email{
          margin-left: 2em;
        }
    </style>
</head>
<body>
  <div class="container">
    <h2 class="heading">Welcome,<%= user.username %></h2>
    <img class="image" src="data:image/jpg;base64,<%= image %>" width="200  " height="200">
    <h1 class="header"> <%= user.username %></h1>
    <p class="email"><%= user.email %></p>
    <button class="button2"><h3>let's start</h3></button>
  </div>
</body>
</html>